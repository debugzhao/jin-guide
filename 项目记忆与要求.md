# 项目记忆与要求汇总

> 本文件汇总了「问津 Agent」项目当前生效的所有**记忆条目**与**硬性要求**，来源有三：
> 1. 全局 `~/.claude/CLAUDE.md`（跨项目个人偏好）
> 2. 项目 `CLAUDE.md`（签入仓库的工程约束）
> 3. 项目记忆库 `memory/`（14 条 feedback / project / reference）
>
> 生成日期：2026-08-12。记忆库随会话持续更新，本文件是某一时刻的快照，非实时同步。

---

## 一、总览（一句话速查）

| 类别 | 条目 | 核心要求 |
|------|------|----------|
| 🗣️ 沟通 | 回复语言 | 所有回复（含执行过程）必须用**中文** |
| 🗣️ 沟通 | 回复格式 | 金字塔原理：先结论后展开；多维信息优先用**表格**；emoji 适度 |
| 💻 编码 | 注释风格 | 中文 + 业务视角，解释「为什么」而非翻译代码 |
| 💻 编码 | 明确 bug 直接修 | 安全、机械性的缺陷当场改，不要只写进记忆当「下次提醒」 |
| 🎨 前端 | 视觉执行红线 | 写 UI 前必查 `frontend-prd-v2.md §3.6`，禁止绕开设计系统写任意值 |
| 📝 文档 | 设计文档视角 | 「设计 XX 模块」默认产出**产品/业务视角**，禁止代码/表结构/技术选型 |
| 📝 文档 | 疑问杂项格式 | 问题总结为一级标题，内容用二级标题展开 |
| 📝 文档 | 问题类六段结构 | 现状/根因/影响/业务价值/解决方案/验收标准，解决方案要面试深度 |
| 🔀 Git | 大改动主动给 commit | 大功能改动后主动总结 + 给 ≤16 字 commit message 草稿，不代为提交 |
| 🚀 部署 | 匿名卷坑 | 已修复：`deploy.sh` 已加 `-V`，新增前端依赖不再报 Module not found |
| 🔧 项目 | 前后端字段映射 | 后端 snake_case，前端 `api.ts` 层手动转 camelCase |
| 🌐 运维 | 证书续期 | `tysonzhao.com` 证书 **2026-10-02** 到期，提前一周（09-25 起）提醒 |
| 🌐 运维 | 域名未备案 | 手机移动数据打不开是未备案被拦截，非技术故障，暂不处理 |
| 🌐 运维 | 生产拓扑 | jdy_server 部署速查（nginx 非 compose 管理、热重载架构） |
| 📊 进度 | 项目进度 | Day 1–8 已完成，详见对应章节 |

---

## 二、沟通与协作要求（feedback）

### 2.1 回复语言 —— 必须用中文
无论描述执行过程、总结变更还是解释代码，**一律用中文**，不能用英文回复。
> 用户原话：「用中文回答你的执行过程，不要用英文回答」。

### 2.2 回复格式 —— 金字塔 + 表格
回答复杂问题或内容较多时，禁止不分重点地大段平铺：
- 先用一两句话给出**结论/概要**，再分层展开细节
- 适合列举、对比、多维度信息时优先用**表格**，而非纯文字堆砌
- 可**适度**用 emoji 增强可读性与重点提示，不要过度

### 2.3 大改动后主动给 commit message
完成一次较大功能改动（新增模块 / 跨 5+ 文件重构 / 影响面广的 bug 修复）后，**不等用户开口**，主动总结并给一条可直接用的 commit message 草稿。
- 只给**一行简短标题**（≤16 字），不带 `feat:`/`fix:` 前缀，**不要**补充说明段落
- 结尾固定加 `Co-Authored-By: Claude ... <noreply@anthropic.com>`
- 只给文本，**不代为执行** `git commit`；完整 add/commit/push 走 `/commit` 命令
- 单行修复、样式微调等琐碎改动**不**触发

---

## 三、编码要求（feedback）

### 3.1 注释风格 —— 中文 + 业务视角
注释用中文，从业务角度解释「为什么」，而不是翻译代码在做什么。
> 反例：「调用 LLM 生成推荐理由」
> 正例：「LLM 负责生成对考生友好的自然语言解释，规则引擎已给出确定性结论」

写注释前自问：这是在解释代码逻辑（不需要），还是在解释业务背景 / 约束原因 / 非显而易见的权衡（需要）？只写后者。

### 3.2 明确 bug 直接修，别推给记忆
遇到**明确、安全、机械性**的代码 / 脚本缺陷（范围清楚、影响可推理、无歧义），**当场直接修复**，不要只写成记忆条目指望「下次自动提醒」。
- 修复明确、纯 bug/坑 → 直接改
- 涉及产品决策、行为取舍、影响范围不确定 → 才先问用户
- 「会影响自动化流程」本身不构成必须先问的理由

---

## 四、前端要求（feedback）

### 4.1 视觉执行红线
写 / 重构前端 UI 组件前，必须对照 `docs/frontend-prd-v2.md §3.6`「视觉执行红线」五条强制规则检查：
1. 阴影分三级：`shadow-sm` / `shadow-floating` / `shadow-modal`，不要全部复用同一等级
2. 辅助色 `#0D9488` 必须真正落地使用，不能只在 config 声明却从不调用
3. 禁止硬编码任意值圆角 / 字号（`rounded-2xl`、`text-[13px]`），用语义 token（`rounded-bubble` 等）
4. 纯文本 AI 回复不强制套边框卡片
5. 侧栏背景需与主内容区有区分

配套：涉及配色 / 圆角 / 阴影 / 字号改动，先检查 `tailwind.config.ts` 有无对应 token，没有先扩展 config，**不允许「先写死、以后再统一」**。整体「纯白克制」基调是产品方主动选择，方向不用推翻，只需把设计系统真正落地。

---

## 五、文档写作要求（feedback）

### 5.1 「设计 XX 模块」默认产品视角
用户说「设计 XX 功能/模块」而未明确说「技术方案/实现方案」时，**默认产出产品视角文档**：用户痛点/场景、业务内容、可感知的体验设计、边界与红线、分阶段优先级、如何衡量做得好。
- **禁止**放代码、文件路径、数据库表结构、具体技术选型（LangGraph Store / Checkpointer / Mem0 等库名）
- 技术选型 / 代码路径 / 数据模型留到用户明确要「技术方案 / 实施计划」（通常走 Plan Mode）再展开

### 5.2 `docs/疑问杂项.md` 标题格式
- 每个问题提炼成**总结性一级标题**（`#`），不能照抄用户原话当标题
- 该问题下的结论、分点、表格一律用**二级标题**（`##`）展开，不从三级起步

### 5.3 「问题类」条目六段结构
在文档里记录**问题 / 故障 / 性能缺陷**类条目时，用固定六段（每段一个二级标题）：
1. **现状** —— 有数据放数据（压测/监控/复现步骤），不写「很慢」这种定性
2. **为什么会出现** —— 讲根因 + 调用链路；被数据证伪的其他猜测要写出排除过程
3. **带来什么影响** —— 分用户/产品/成本/技术债等维度
4. **业务价值** —— 挂钩产品定位 / 转化漏斗 / 商业指标
5. **解决方案** —— **资深工程师面试深度**：方法论、备选方案与权衡、预判追问、分阶段落地；不能只列一个优化建议表格
6. **验收标准** —— 具体可衡量 + 说明用什么方法验证，不瞎编数字

> 仅**问题/故障/性能类**条目套此结构；纯知识科普 / 选型对比类按内容自然组织即可。

---

## 六、项目状态与约定（project）

### 6.1 前后端字段映射
后端 API 全部 snake_case，前端 `api.ts` 在调用层手动转 camelCase：

| 接口 | 后端字段 | 前端字段 |
|------|----------|----------|
| POST /auth/session | session_id | sessionId |
| POST /profile | id | profileId |
| POST /reports/generate | run_id | runId |
| POST /reports/generate 请求体 | profile_id | (从 profileId 转换) |

新增 API 方法时手动转换字段名，不能直接用泛型 `apiFetch<T>` 返回 raw 类型。

### 6.2 项目进度（Day 1–8 已完成）
| Day | 内容 | 状态 |
|-----|------|------|
| Day 1 | 基础设施 + 骨架 | ✅ |
| Day 2 | 后端 API + Worker | ✅ |
| Day 3 | 前端骨架 + M1 端到端联调 | ✅ 2026-06-30 |
| Day 4 | 内容数据 + 规则引擎 + 工具可靠性基础 | ✅ 2026-07-01 |
| Day 5 | 推荐算法 + 风险引擎 | ✅ 2026-07-01 |
| Day 6 | RAG Pipeline（pgvector + rerank） | ✅ 2026-07-01 |
| Day 7 | LangGraph 真实编排 + M2 联调 | ✅ 2026-07-01 |
| Day 8 | Reflection Agent + HITL 后端 | ✅ 2026-07-01 |

> 注：HITL（人工复核）后端在 Day 8 交付，但项目 `CLAUDE.md` 标注**已在 v1.1 移除**——记忆库进度条目为历史快照，以 `CLAUDE.md`「已移除（v1.1）」为准。

各 Day 详细交付物与技术约定见记忆文件 `project_progress.md`（含规则引擎同步 Session、f-string 禁中文弯引号、pgvector cosine_distance 等关键约定）。

---

## 七、运维 / 生产环境（project / reference）

### 7.1 生产服务器 jdy_server 拓扑速查
- 代码在 `/opt/wenjin`，**非 git 仓库**，靠 `scripts/deploy.sh` rsync + 远程 `docker compose build/up -d`
- `backend`/`frontend` 容器均**开发模式挂载源码 + 热重载**，文件落盘即生效（已知脆弱点，非不可变镜像部署）
- Nginx 是**独立容器 `codex-nginx`（手动 `docker run`，非 compose 管理）**，配置在 `/opt/codex-nginx/`；改 `docker run` 级参数需整个重建容器，重建后要重新 `docker network connect` 回 `nginx_proxy_net` 和 `wenjin_default`
- 反代路由：`/`（静态博客）、`/wenjin/` → frontend:3000、`/wenjin-api/` → backend:8000、`/portainer/` → portainer:9000
- 改动前建议先 `cp -r /opt/codex-nginx /opt/codex-nginx.bak.$(date +...)` 备份
- SSH 走 `mcp__ssh-manager__*` 工具，IP `117.72.127.159`，root + key 认证

### 7.2 ⚠️ HTTPS 证书续期提醒
`tysonzhao.com` 证书有效期到 **2026-10-02**，部署在 `codex-nginx`，文件在 `/opt/codex-nginx/certs/tysonzhao.com/`（`.crt` 644 / `.key` 600）。
- **当前日期临近或超过 2026-09-25（到期前一周）时，主动提醒用户续期**
- 续期：新证书替换 `.crt`/`.key` 即可，路径不变无需改配置，稳妥起见 `docker exec codex-nginx nginx -s reload`
- 提醒依赖记忆被读取，非主动推送——用户也应在日历独立设一条

### 7.3 域名未备案（已知问题，暂不处理）
`tysonzhao.com` 部署在大陆服务器（京东云北京，电信线路）但**未完成 ICP 备案**。现象：直接 IP 访问正常，域名访问在国内移动数据（4G/5G）会被运营商拦截，家庭宽带暂时可访问。
- 这是国内网络监管要求，**非服务器/nginx 故障**
- 2026-07-05 与用户确认现阶段**暂不处理**；若之后用户提「手机打不开」，直接告知这是已知未备案限制，不要当技术故障排查

### 7.4 前端匿名卷部署坑（已修复）
`docker-compose.yml` 的 `frontend` 服务用匿名卷隔离 `node_modules`。曾因 recreate 复用旧匿名卷，导致新增前端依赖后线上报 `Module not found`。
- **已于 2026-07-18 修复**：`scripts/deploy.sh` 部署命令统一加 `-V`（backend/worker 无匿名卷，全量加 `-V` 安全）
- 以后正常跑 `./scripts/deploy.sh` 不再复现，**无需手动补** `--force-recreate -V`

---

## 八、来源文件索引

| 记忆文件 | 类型 | 对应本文档章节 |
|----------|------|----------------|
| `feedback_language.md` | feedback | 2.1 |
| `feedback_commit_message_on_major_change.md` | feedback | 2.3 |
| `feedback_comments.md` | feedback | 3.1 |
| `feedback_fix_dont_defer_to_memory.md` | feedback | 3.2 |
| `feedback_frontend_design_execution_rules.md` | feedback | 4.1 |
| `feedback_design_docs_product_not_tech.md` | feedback | 5.1 |
| `feedback_doubts_doc_heading_format.md` | feedback | 5.2 |
| `feedback_issue_doc_six_part_structure.md` | feedback | 5.3 |
| `project_api_mappings.md` | project | 6.1 |
| `project_progress.md` | project | 6.2 |
| `reference_jdy_server_infra.md` | reference | 7.1 |
| `project_cert_renewal.md` | project | 7.2 |
| `project_icp_beian_known_issue.md` | project | 7.3 |
| `project_frontend_anon_volume_deploy_gotcha.md` | project | 7.4 |

> 回复格式偏好来自全局 `~/.claude/CLAUDE.md`（对应 2.2）；项目工程约束（分层架构、确定性系统边界、SSE 鉴权、合规禁词、游标分页等）见仓库 `CLAUDE.md`，本文件未逐条复述。
